# Phase 2 auth-layer stack config. Copy to .env and fill in real secrets —
# never commit a real .env.
#
#   openssl rand -base64 48   # for POSTGRES_PASSWORD and JWT_SECRET

POSTGRES_PASSWORD=change-me
JWT_SECRET=change-me-at-least-32-chars

# Generated from JWT_SECRET above via ../scripts/generate-supabase-keys.mjs
# (run that script after setting JWT_SECRET, then paste its output here).
ANON_KEY=
SERVICE_ROLE_KEY=

# Public URL of this stack once deployed behind Kong (matches the plan's
# eventual reverse-proxy setup, same shape as frontend/'s VITE_API_URL today).
SUPABASE_PUBLIC_URL=http://localhost:8000

# Frontend origin, for GoTrue's SITE_URL / redirect allow-list.
SITE_URL=http://localhost:5173
URI_ALLOW_LIST=http://localhost:5173

# SMTP — optional at this phase (admin-created accounts are pre-confirmed
# via the admin API, not email confirmation). Needed once password-recovery
# emails are wired up. Same deferred-until-set pattern as MEILI_MASTER_KEY
# in backend/docker-compose.yml.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_ADMIN_EMAIL=

WEBAUTHN_RP_NAME=CLUSTR Barangay OS
